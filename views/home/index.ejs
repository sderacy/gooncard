<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>

    <!-- 
      When index.html is loaded, 2 requests will be 
      sent to the server for the CSS and JS files. 
    -->
    <link rel="stylesheet" href="/home/style" />
    <script src="/home/main" type="module"></script>
  </head>
  <body>
    <h1>Hello World!</h1>
    <p>This file was served with Express.</p>
    <p>Here is your information:</p>
    <ul>
      <li>First Name: <%= user.first_name %></li>
      <li>Last Name: <%= user.last_name %></li>
      <li>Email: <%= user.email %></li>
      <li>Password: <%= user.password %></li>
    </ul>

    <!-- Testing requesting an image. -->
    <img src="/hamster.jpg" alt="Hamster image" />

    <!-- Example of writing inline JS with EJS files. -->
    <p>
      The following uses inline JS in an EJS file to print the object's
      key/values:
    </p>
    <ul>
      <% Object.keys(user).forEach( (key) => { %>
      <li><%= key %>: <%= user[key] %></li>
      <% }); %>
    </ul>
    <a href="/account/logout">Logout</a>
  </body>
</html>
